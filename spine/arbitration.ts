/**
 * Priority and conflict resolution
 *
 * Invariants:
 * 1 — Single authority
 * 6 — Background cognition is advisory only
 */

export function arbitrate(_consciousSnapshot: any[]): any[] {
  // placeholder: no decisions yet
  return [];
}
