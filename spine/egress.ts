/**
 * Forgetting and demotion
 *
 * Invariants:
 * 8 — Forgetting is the default
 */

import { ConsciousWorkspace } from "./conscious";

export function egress(_conscious: ConsciousWorkspace): void {
  // placeholder: forgetting logic comes later
}
