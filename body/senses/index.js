/**
 * Senses entrypoint.
 * MUST only observe and return descriptive events (no interpretation).
 */
export async function collectSenseEvents() {
  // Placeholder: return no observations yet
  return [];
}

